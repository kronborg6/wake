[package]
name = "dwake"
version = "0.0.1"
edition = "2024"

[dependencies]
common = { path = "../../crates/common" }
docker = { path = "../../crates/docker" }
cli = { path = "../../crates/cli" }
tui = { path = "../../crates/tui" }

tokio = { version = "1", features = ["full"] }
